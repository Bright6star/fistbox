<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc 
						http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
						http://www.springframework.org/schema/beans 
						http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/context 
						http://www.springframework.org/schema/context/spring-context-4.2.xsd">


	<mvc:annotation-driven/>
	<context:component-scan base-package="action"/>
	
	<mvc:default-servlet-handler/>
	<mvc:view-controller path="/" view-name="redirect:/index"/>
	<mvc:view-controller path="/admin" view-name="forward:/admin/login"/>
	 <!-- 配置FreeMarker的模板路径和其它参数 -->
	 <!-- 需要导入spring-context-support-4.2.5.RELEASE.jar -->
	 <bean class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
	 	<!-- 指定freemarker模板所在文件夹（前缀） -->
	 	<property name="templateLoaderPaths" value="/pages/,/WEB-INF/admin/"/>
	 	<!-- 指定模板编码 -->
	 	<property name="defaultEncoding" value="UTF-8"/>
	 </bean>
	 
	 <!-- Freemarker视图解析器 -->
	<bean class="org.springframework.web.servlet.view.freemarker.FreeMarkerViewResolver">
		<!-- 指定视图后缀 -->
		<property name="suffix" value=".html"/>
		<!-- 指定模板文件中编码 -->
		<property name="contentType" value="text/html;charset=UTF-8"/>
		<!-- 将HttpServletReques对象使用request变量名进行映射，方便在freemarker页面使用 -->
		<property name="requestContextAttribute" value="request"/>
	</bean>
	<!-- 后台拦截器 -->
	<bean id="loginInterceptor" class="interceptor.LoginInterceptor"/>
	
	<bean id="fistloginInterceptor" class="interceptor.FistLoginInterceptor"/>
	
	<!-- 文件上传配置 -->
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="defaultEncoding" value="UFT-8"/>
		<property name="maxUploadSize" value="5848220"/>
	</bean>
	
	<mvc:interceptors>
		<mvc:interceptor>
		<!-- 配置需要拦截的请求 -->
			<mvc:mapping path="/admin/*/*"/>
			<!-- 配置不需要配置的请求 -->
			<mvc:exclude-mapping path="/admin/"/>
			<ref bean="loginInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>
	
	<mvc:interceptors>
		<mvc:interceptor>
		<!-- 配置需要拦截的请求 -->
			<mvc:mapping path="/saveOrder"/>
			<mvc:mapping path="/orderAlipay"/>
			<mvc:mapping path="/directPayResult"/>
			<mvc:mapping path="/orderDetails"/>
			<mvc:mapping path="/smOrder"/>
			<mvc:mapping path="/saveOrder"/>
			<!-- 配置不需要配置的请求 -->
			<mvc:exclude-mapping path="/login"/>
			<mvc:exclude-mapping path="/index"/>
			<mvc:exclude-mapping path="/detail"/>
			<mvc:exclude-mapping path="register"/>
			<mvc:exclude-mapping path="/active"/>
			<mvc:exclude-mapping path="invalidate"/>
			<ref bean="fistloginInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>
		
</beans>
